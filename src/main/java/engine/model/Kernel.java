package engine.model;

import java.util.List;
import java.util.ArrayList;

// import engine.model.graphicalEngine.RectangleDrawing;
import engine.model.physicalEngine.shape.*;
import javafx.scene.paint.Color;

public class Kernel {
    // private List<Shape> shapes = new ArrayList<>();

    // public void addShape(Shape shape) {
    //     this.shapes.add(shape);
    // }

    // public List<Shape> getShapes() {
    //     return this.shapes;
    // }

    // public void removeShape(Shape shape) {
    //     this.shapes.remove(shape);
    // }

    // public void clearShapes() {
    //     this.shapes.clear();
    // }

    // public void drawAll() {
    //     for (Shape shape : this.shapes) {
    //         shape = (Rectangle) shape;
    //         // RectangleDrawing rectangleDrawing = new RectangleDrawing(shape.getPosition().getX(), shape.getPosition().getY(), shape.getWidth(), shape.getLength(), Color.BLUE);
    //     }
    // }
    
}
