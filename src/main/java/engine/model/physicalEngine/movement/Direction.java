package engine.model.physicalEngine.movement;

/**
 * This enum is used to represent the direction of a movement. This is basic movements (up, down, left, right) and none if there is no movement.
 */

public enum Direction {
    UP, DOWN, LEFT, RIGHT, NONE;
}
